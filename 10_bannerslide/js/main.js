$(function () {
    $('.list').slick({
        autoplay: true,
        // 한화면에 보일 개수
        slidesToShow:2,
        slidesToScroll:1,
        arrows:false,
        // 페이지버튼 보이기
        dots:true,
    });
})